// Audit single file placeholder
import { validateInvariants } from '../lib/audio/features/invariants.js';
import fs from 'node:fs';

console.log('[audit:one] Placeholder – implementação futura: carregar WAV, extrair métricas existentes (loudness, true peak, spectrum) e gerar JSON + invariants.');
console.log('Flags AUDIT_MODE=', process.env.AUDIT_MODE);

// TODO: implementar pipeline real
